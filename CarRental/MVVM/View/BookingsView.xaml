<UserControl x:Class="CarRental.MVVM.View.BookingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CarRental.MVVM.View" xmlns:local1="clr-namespace:CarRental"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <UserControl.Resources>
        <!-- Стиль для кнопок -->
        <Style x:Key="ButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#252A37"/>
            <Setter Property="Foreground" Value="#FFFFFF"/>
            <Setter Property="Padding" Value="5"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="10">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <!-- Верхняя строка для кнопок -->
            <RowDefinition Height="Auto"/>

            <RowDefinition Height="Auto"/>
            <!-- Остальное пространство для отображения карточек -->
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Верхняя панель с кнопками -->
        <StackPanel HorizontalAlignment="Center" Grid.Row="0" Orientation="Horizontal" Margin="5">
            <TextBlock Margin="-70,0,0,0" Text="Bookings" FontSize="20" Foreground="White" FontWeight="Medium"/>
        </StackPanel>


        <!-- Область с метками -->
        <Grid Grid.Row="1">
            <StackPanel Orientation="Horizontal" Margin="5">
                <TextBlock Foreground="#fff" Text="Image" FontWeight="ExtraLight" Margin="40,0,10,0"/>
                <TextBlock Foreground="#fff" Text="Brand" FontWeight="ExtraLight" Margin="60,0,30,0"/>
                <TextBlock Foreground="#fff" Text="Model" FontWeight="ExtraLight" Margin="10,0,40,0"/>
                <TextBlock Foreground="#fff" Text="Renter" FontWeight="ExtraLight" Margin="0,0,30,0"/>
                <TextBlock Foreground="#fff" Text="Renter email" FontWeight="ExtraLight" Margin="0,0,30,0"/>
                <TextBlock Foreground="#fff" Text="Price/24h" FontWeight="ExtraLight" Margin="0,0,30,0"/>
            </StackPanel>
        </Grid>

        <!-- Область для отображения карточек -->
        <ScrollViewer Grid.Row="2" HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Hidden">
            <ItemsControl ItemsSource="{Binding Bookings}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel Orientation="Vertical"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>

                    <DataTemplate>
                        <Border Width="650" Height="100" Background="#252A37" CornerRadius="15" Margin="5">
                            <StackPanel Orientation="Horizontal" Margin="5" VerticalAlignment="Center">

                               
                                <Image Source="{Binding Car.ImagePath}" Width="110" Height="50"/>

                                <TextBlock Text="{Binding Car.Brand}" FontSize="14" Foreground="White" TextTrimming="None" Margin="10,10,22,10" VerticalAlignment="Center"/>

                                <TextBlock Text="{Binding Car.Model}" FontSize="14" Foreground="White" TextTrimming="WordEllipsis" Margin="10,10,22,10" VerticalAlignment="Center"/>

                                <TextBlock Text="{Binding  User.UserName}" FontSize="14" Foreground="White" TextTrimming="WordEllipsis" Margin="10,10,22,10" VerticalAlignment="Center"/>

                                <TextBlock Text="{Binding User.Email}" FontSize="7" Foreground="White" TextWrapping="Wrap" Margin="10,10,22,10" VerticalAlignment="Center" MaxWidth="200"/>

                                <TextBlock Text="{Binding Car.Price}" FontSize="14" Foreground="White" TextTrimming="WordEllipsis" Margin="10,10,22,10" VerticalAlignment="Center"/>

                                <Button Width="100" Height="25" Content="Delete" Margin="10,10,10,5" Command="{Binding DataContext.DeleteBookingCommand, RelativeSource={RelativeSource AncestorType={x:Type UserControl}}}" CommandParameter="{Binding}"  Style="{StaticResource ButtonStyle}"/>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</UserControl>
