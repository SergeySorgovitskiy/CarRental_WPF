<UserControl x:Class="CarRental.MVVM.View.EditCarView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CarRental.MVVM.View" xmlns:viewmodel="clr-namespace:CarRental.MVVM.ViewModel" d:DataContext="{d:DesignInstance Type=viewmodel:AddCarViewModel}"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="20">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Left Column -->
        <StackPanel Grid.Column="0" Margin="0,0,20,0">
            <Label Content="Brand" Foreground="White" Margin="50,10,0,0"/>
            <TextBox x:Name="TextBoxBrand" Text="{Binding Brand}" Width="200" Background="#252A37" Foreground="White" Padding="5" Margin="0,5,0,0" BorderThickness="0"/>

            <Label Content="Model" Foreground="White" Margin="50,10,0,0"/>
            <TextBox x:Name="TextBoxModel" Text="{Binding Model}" Width="200" Background="#252A37" Foreground="White" Padding="5" Margin="0,5,0,0" BorderThickness="0"/>

            <Label Content="Year" Foreground="White" Margin="50,10,0,0"/>
            <TextBox x:Name="TextBoxYear" Text="{Binding Year}" Width="200" Background="#252A37" Foreground="White" Padding="5" Margin="0,5,0,0" BorderThickness="0"/>

            <Label Content="Body Type" Foreground="White" Margin="50,10,0,0"/>
            <TextBox x:Name="TextBoxBodyType" Text="{Binding BodyType}" Width="200" Background="#252A37" Foreground="White" Padding="5" Margin="0,5,0,0" BorderThickness="0"/>

            <Label Content="Price" Foreground="White" Margin="50,10,0,0"/>
            <TextBox x:Name="TextBoxPrice" Text="{Binding Price}" Width="200" Background="#252A37" Foreground="White" Padding="5" Margin="0,5,0,0" BorderThickness="0"/>
        </StackPanel>

        <!-- Right Column -->
        <StackPanel Grid.Column="1">
            <Label Content="Description" Foreground="White" Margin="60,10,0,0"/>
            <TextBox x:Name="TextBoxDescription" Text="{Binding Description}" Width="200" Height="100" Background="#252A37" Foreground="White" Padding="5" Margin="0,5,0,0" BorderThickness="0" TextWrapping="Wrap" AcceptsReturn="True"/>

            <Button Content="Add Photo" Width="200" Height="40" Background="#314FF6" Foreground="White" Margin="0,20,0,0" Command="{Binding ChooseImageCommand}" />
        </StackPanel>

        <!-- Bottom Buttons -->
        <StackPanel Orientation="Horizontal" Grid.ColumnSpan="2" HorizontalAlignment="Center" Margin="0,20,0,0" Grid.Row="6">
            <Button Content="Save" Width="150" Height="40" Background="#314FF6" Foreground="White" Margin="10" Command="{Binding SaveCommand}"/>
            <Button Content="Cancel" Width="150" Height="40" Background="Gray" Foreground="White" Margin="10" Command="{Binding CancelCommand}" />
        </StackPanel>
    </Grid>
</UserControl>
